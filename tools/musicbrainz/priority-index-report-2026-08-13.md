# AURA priority artist index

Generated automatically from the local full MusicBrainz index (`20260812-001001`).

| Metric | Result |
|---|---:|
| ListenBrainz current top rows | 964 matched MBIDs |
| Regional countries | AZ, RU, UA, KZ, BY, AM, GE, MD, KG, UZ, TJ, TM, TR |
| Selected artists | 42,215 |
| Aliases | 20,111 |
| SQLite asset | 14,368,768 bytes (13.7 MiB) |
| APK before priority index | 52,749,983 bytes (50.3 MiB) |
| APK with priority index | 60,226,984 bytes (57.4 MiB) |
| APK delta | 7,477,001 bytes (7.1 MiB) |
| Exact/prefix/FTS median | 0.007 / 4.003 / 0.019 ms |
| Exact/prefix/FTS p95 | 0.011 / 4.378 / 0.034 ms |

The build used ListenBrainz automatically. YouTube regional chart enrichment is supported by `build_priority_index.py` through `--youtube-key`, but was not enabled for this build because no YouTube Data API key is configured. No chart/video metadata is stored in the Android asset.

The APK contains the priority index; the full 2.96-million-artist database remains a local build input and is not committed.
